package com.example.androideseo.service

import com.example.androideseo.data.models.Client

class ServiceClient {


   /* suspend fun connexion(user:String,mdp:String): Client {
        return serviceAPIclient.instance.connexion(page).data.map {
            LocalUser(
                    it.id,
                    it.email,
                    it.first_name,
                    it.last_name,
                    it.avatar
            )
        }
    }

    */
}